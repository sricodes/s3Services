import com.amazonaws.services.s3.transfer.Upload;

import java.io.File;

public class Uploader {

    public static void upload(){
     /*   File f = new File(local_src);
        Upload upload= transferManager.upload(bucketName,"OUTPUT"+"/"+"processed.csv",f);
        upload.waitForUploadResult();
        context.getLogger().log("" + upload.getState());
        transferManager.shutdownNow();*/
    }
}
